# Deployment

- docker/  — Dockerfiles and compose configs for backend/frontend/database
- ci/      — CI/CD pipeline configs (build, test, deploy)

Nothing here yet — the MVP runs locally via `streamlit run app.py`.
